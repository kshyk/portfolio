| Model | Audit function | File opened? | Import path | DI dependencies observed | Decision (Branch 1/2/3) | Notes |
|-------|----------------|--------------|-------------|--------------------------|-------------------------|-------|
| Skill | static JSON import (build-time, no runtime creation) | yes, data/skills.json lines 1-59 | `import skillsData from '@/data/skills.json'` | None -- pure static JSON file, no service class, no DB client, no side effects | Branch 2 -- import existing static export and return data | No runtime creation function exists. The "creation path" is the static file itself. Factory returns individual skill objects from the imported JSON. No ORM, no DB, no extraction needed. |
| Project | static JSON import (build-time, no runtime creation) | yes, data/projects.json lines 1-102 | `import projectsData from '@/data/projects.json'` | None -- pure static JSON file, no service class, no DB client, no side effects | Branch 2 -- import existing static export and return data | No runtime creation function exists. Factory returns individual project objects from the imported JSON array. No ORM, no DB, no extraction needed. |
| SiteConfig | static TypeScript export (build-time, no runtime creation) | yes, config/site.ts lines 1-14 | `import { siteConfig } from '@/config/site'` | None -- pure TypeScript constant, no service class, no DB client, no side effects | Branch 2 -- import existing static export and return data | Singleton config object. Factory returns the siteConfig object. No ORM, no DB, no extraction needed. |
